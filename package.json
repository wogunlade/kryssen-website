{
  "name": "kryssen-website",
  "version": "6.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "preview": "wrangler versions upload",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.86.0"
  }
}
